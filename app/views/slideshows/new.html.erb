<h1>Create new slideshow</h1>

<%= form_for @slideshow do |f| %>

  <%= f.label :name %><br />
  <%= f.text_field :name %>
  <%= f.label :shared %>
  <%= f.check_box :shared %>

  <%= f.submit "Create", :disable_with => 'Creating...' %>

<% end %>
